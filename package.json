{
  "name": "dload",
  "version": "0.0.1",
  "description": "Downloader for server",
  "author": "fuzzknob",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "aria": "turbo run dev --scope=aria",
    "client": "turbo run dev --scope=client",
    "server": "turbo run dev --scope=server"
  },
  "dependencies": {
    "turbo": "^1.7.0"
  }
}
